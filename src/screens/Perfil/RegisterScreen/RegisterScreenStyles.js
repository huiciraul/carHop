import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    Image:{
        resizeMode: "contain",
        width:"100%",
        height: 300,
        marginTop: 20,
    },
    content:{
        marginHorizontal: 40,

    },
    
})